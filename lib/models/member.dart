class Member {
  String name;
  String email;
  String imageUrl;
  String role;
  bool isEditable;

  Member({
    required this.name,
    required this.email,
    required this.imageUrl,
    required this.role,
    required this.isEditable,
  });
}